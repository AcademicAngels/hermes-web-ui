services:
  hermes-agent:
    image: ${HERMES_AGENT_IMAGE:-nousresearch/hermes-agent:latest}
    container_name: ${HERMES_AGENT_CONTAINER_NAME:-hermes-agent}
    volumes:
      - ${HERMES_DATA_DIR:-./hermes_data}:/home/agent/.hermes
      - hermes-agent-src:/opt/hermes
    environment:
      - HERMES_HOME=/home/agent/.hermes
      - TZ=${TZ:-Asia/Shanghai}
      - HERMES_TIMEZONE=${HERMES_TIMEZONE:-Asia/Shanghai}
      - HINDSIGHT_MODE=${HINDSIGHT_MODE:-}
      - HINDSIGHT_API_URL=${HINDSIGHT_API_URL:-http://hindsight:8888}
    stdin_open: true
    tty: true
    restart: unless-stopped
    ports:
      - "8642-8670:8642-8670"
    networks:
      - hindsight-net

  hermes-webui:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BASE_IMAGE: ${HERMES_AGENT_IMAGE:-nousresearch/hermes-agent:latest}
    image: ${WEBUI_IMAGE:-hermes-web-ui-local:latest}
    container_name: ${WEBUI_CONTAINER_NAME:-hermes-webui}
    entrypoint: ["node", "dist/server/index.js"]
    depends_on:
      - hermes-agent
    ports:
      - "${PORT:-6060}:${PORT:-6060}"
    volumes:
      - ${HERMES_DATA_DIR:-./hermes_data}:/home/agent/.hermes
      - ${HERMES_DATA_DIR:-./hermes_data}/hermes-web-ui:/home/agent/.hermes-web-ui
      - hermes-agent-src:/opt/hermes
    environment:
      - PORT=${PORT:-6060}
      - UPSTREAM=${UPSTREAM:-http://hermes-agent:8642}
      - HERMES_HOME=/home/agent/.hermes
      - TZ=${TZ:-Asia/Shanghai}
      - HERMES_TIMEZONE=${HERMES_TIMEZONE:-Asia/Shanghai}
      - HERMES_BIN=${HERMES_BIN:-/opt/hermes/.venv/bin/hermes}
      - AUTH_DISABLED=${AUTH_DISABLED:-false}
      - HINDSIGHT_MODE=${HINDSIGHT_MODE:-}
      - HINDSIGHT_API_URL=${HINDSIGHT_API_URL:-http://hindsight:8888}
      - HINDSIGHT_URL=${HINDSIGHT_URL:-http://hindsight:8888}
    restart: unless-stopped
    networks:
      - hindsight-net

  hermes-cron-memory-ingestor:
    build:
      context: ${CRON_MEMORY_INGESTOR_CONTEXT:-../hermes-cron-memory-ingestor}
      dockerfile: Dockerfile
    image: ${CRON_MEMORY_INGESTOR_IMAGE:-hermes-cron-memory-ingestor-local:latest}
    container_name: ${CRON_MEMORY_INGESTOR_CONTAINER_NAME:-hermes-cron-memory-ingestor}
    depends_on:
      - hindsight
    volumes:
      - ${HERMES_DATA_DIR:-./hermes_data}:/home/agent/.hermes
    environment:
      - HERMES_HOME=/home/agent/.hermes
      - CRON_OUTPUT_DIR=/home/agent/.hermes/cron/output
      - HINDSIGHT_URL=${HINDSIGHT_URL:-http://hindsight:8888}
      - HINDSIGHT_BANK_ID=${HINDSIGHT_BANK_ID:-default}
      - INGEST_INTERVAL_SECONDS=${CRON_MEMORY_INGEST_INTERVAL_SECONDS:-60}
      - INGESTOR_EVENT_HOST=0.0.0.0
      - INGESTOR_EVENT_PORT=${CRON_MEMORY_EVENT_PORT:-8787}
      - INGESTOR_EXCLUDED_SESSION_IDS=${CRON_MEMORY_EXCLUDED_SESSION_IDS:-dfff2bec-c29e-4e04-a9cb-dd83b451bf44}
      - INGESTOR_TAGS=${CRON_MEMORY_TAGS:-hermes,cron}
    expose:
      - "${CRON_MEMORY_EVENT_PORT:-8787}"
    restart: unless-stopped
    networks:
      - hindsight-net

  hindsight-postgres:
    image: pgvector/pgvector:pg${HINDSIGHT_DB_VERSION:-18}
    container_name: hindsight-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${HINDSIGHT_DB_USER:-hindsight_user}
      POSTGRES_PASSWORD: ${HINDSIGHT_DB_PASSWORD:-hindsight_pass}
      POSTGRES_DB: ${HINDSIGHT_DB_NAME:-hindsight_db}
    volumes:
      - ${HERMES_DATA_DIR:-./hermes_data}/hindsight-postgres:/var/lib/postgresql
    networks:
      - hindsight-net

  hindsight:
    image: ghcr.io/vectorize-io/hindsight:${HINDSIGHT_VERSION:-latest}
    container_name: hindsight-app
    restart: unless-stopped
    ports:
      - "${HINDSIGHT_PORT:-18888}:8888"
      - "${HINDSIGHT_UI_PORT:-19999}:9999"
    environment:
      - HINDSIGHT_API_DATABASE_URL=postgresql://${HINDSIGHT_DB_USER:-hindsight_user}:${HINDSIGHT_DB_PASSWORD:-hindsight_pass}@hindsight-postgres:5432/${HINDSIGHT_DB_NAME:-hindsight_db}
      - HINDSIGHT_API_LLM_API_KEY=${HINDSIGHT_LLM_API_KEY:-}
      - HINDSIGHT_API_LLM_BASE_URL=${HINDSIGHT_LLM_BASE_URL:-}
      - HINDSIGHT_API_LLM_MODEL=${HINDSIGHT_LLM_MODEL:-}
      - HINDSIGHT_API_EMBEDDING_PROVIDER=${HINDSIGHT_EMBEDDING_PROVIDER:-openai_compatible}
      - HINDSIGHT_API_EMBEDDING_BASE_URL=${HINDSIGHT_EMBEDDING_BASE_URL:-}
      - HINDSIGHT_API_EMBEDDING_API_KEY=${HINDSIGHT_EMBEDDING_API_KEY:-}
      - HINDSIGHT_API_EMBEDDING_MODEL=${HINDSIGHT_EMBEDDING_MODEL:-}
    depends_on:
      - hindsight-postgres
    networks:
      - hindsight-net

volumes:
  hermes-agent-src:

networks:
  hindsight-net:
    driver: bridge
